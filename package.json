{
  "name": "nop-lib",
  "version": "1.1.1",
  "license": "Apache-2.0",
  "description": "The Test-Lib",
  "repository": {
    "type": "git",
    "url": "https://github.com/flovogt/test-lib.git"
  },
  "engines": {
    "node": ">= 8.5",
    "npm": ">= 5"
  },
  "devDependencies": {
    "@semantic-release/git": "^7.0.12",
    "@semantic-release/changelog": "^3.0.4",
    "replace-in-file": "4.1.0"
  },
  "scripts": {
    "test": "echo \"Thats my test\" && printenv GIT_AUTHOR_EMAIL && printenv GIT_AUTHOR_NAME && printenv GIT_COMMITTER_EMAIL && printenv GIT_COMMITTER_NAME && exit 0",
    "prepublishOnly": "node ./scripts/builder.js"
  },
  "files": [
    "src/*.js",
    "LICENSE.txt"
  ]
}
